## Context

The evidence UI now exposes the intentional-stop outcome delivered by IS-205.

## Goals / Non-Goals

**Goals:**

- Keep UI-513 traceable in the OpenSpec task record.

**Non-Goals:**

- Duplicate the evidence artifact contract.

## Decisions

- Archive a `skip_specs` record because the behaviour specification is owned by
  the linked IS-205 change.

## Risks / Trade-offs

- The record intentionally links to IS-205 rather than repeating its delta.
