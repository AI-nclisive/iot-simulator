## 1. Modbus template catalog

- [x] 1.1 Add the SunSpec Common Model and Three-Phase Inverter Model 103
      template with explicit zero-based holding-register bindings.
- [x] 1.2 Expose the profile through the catalog's template map and ordered
      template-name list.

## 2. Tests and verification

- [x] 2.1 Add unit coverage for catalog discovery, representative SunSpec
      addresses/types, and non-overlapping multi-register bindings.
- [ ] 2.2 Run the affected module tests and `./gradlew build`.
