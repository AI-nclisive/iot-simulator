# Base URL for the backend API (used in production builds).
# In development the Vite dev proxy rewrites /api → http://localhost:8080,
# so this variable is only needed when deploying the built static assets.
VITE_API_BASE_URL=http://localhost:8080
