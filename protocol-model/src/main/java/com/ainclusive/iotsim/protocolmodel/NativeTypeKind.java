package com.ainclusive.iotsim.protocolmodel;

/** The value-shape categories represented by an OPC UA native type declaration. */
public enum NativeTypeKind {
    ENUM,
    OPTION_SET,
    STRUCTURE,
    UNION,
    OPAQUE
}
