name: Task
description: A task (backend, frontend, or repo/process) tracked by an IS-ID from backend-specs/TASKS.md
title: "IS-XXX [BE] "
labels: ["type:task"]
body:
  - type: markdown
    attributes:
      value: |
        Title format: **`IS-XXX [AREA] short name`** — replace `IS-XXX` with the task id and
        set the `[AREA]` marker in the title to match the **Area** you pick below
        (`[BE]` / `[FE]` / `[SDLC]`).
  - type: input
    id: task-id
    attributes:
      label: Task ID
      description: IS-ID from backend-specs/TASKS.md, or "new" if not yet cataloged.
      placeholder: IS-039
    validations:
      required: true
  - type: dropdown
    id: area
    attributes:
      label: Area
      description: Project area — mirror it in the title marker ([BE] / [FE] / [SDLC]).
      options:
        - BE
        - FE
        - SDLC
    validations:
      required: true
  - type: dropdown
    id: module
    attributes:
      label: Module
      description: Subsystem this task belongs to (mainly for backend tasks; optional).
      options:
        - build
        - persistence
        - model
        - ipc
        - runtime
        - project
        - source
        - schema
        - recording
        - replay
        - gen
        - observability
        - api
        - auth
        - io
        - sdlc
  - type: dropdown
    id: priority
    attributes:
      label: Priority / wave
      options:
        - "P0 (Wave A/B/C)"
        - "P1 (Wave D/E)"
        - "P2 (Wave F)"
        - "SDLC"
        - "n/a"
    validations:
      required: true
  - type: textarea
    id: goal
    attributes:
      label: Goal
      description: What this task delivers and why.
    validations:
      required: true
  - type: textarea
    id: acceptance
    attributes:
      label: Acceptance criteria
      description: Definition of done — behavior, tests, `./gradlew build` green.
    validations:
      required: true
  - type: input
    id: depends
    attributes:
      label: Depends on
      description: IS-IDs that must land first (if any).
      placeholder: IS-035
  - type: textarea
    id: notes
    attributes:
      label: Notes
